projection;
strict ( 2 );
use draft;

define behavior for ZC_AJ_GENRES //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Resume;
  use action Discard;
  use action Activate;
  use action Prepare;
}